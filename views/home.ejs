<%- include('partials/header'); -%>
  <p>
    <%= homeContent %>
  </p>

  <!-- Using a for loop -->
  <!-- <% for (let i=0; i < posts.length; i ++) {%>
    <h5>Title</h5>
    <p>
      <%= posts[i].title %>
    </p>
    <h5>Post</h5>
    <p>
      <%= posts[i].message %>
    </p>
    <% }%> -->

  <!-- Using a forEach loop -->
  <% posts.forEach(post=> { %>

    <h5>
      <%= post.title %>
    </h5>
    <p>
      Post: <%= post.message %>
    </p>
    <% }); %>
      <%- include('partials/footer'); -%>